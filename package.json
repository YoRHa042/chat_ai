{
  "name": "chat-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered voice & video call app with anime character voice",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:tts": "cd python-tts && python tts_server.py",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd ../python-tts && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
